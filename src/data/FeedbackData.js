const FeedbackData = [
    {
        id: 1, 
        rating: 8, 
        text: "A mock API server or mock server API imitates a real API server by providing realistic mock API responses and simulate the data the real API would return, matching the schema with data types, objects, and arrays."
    },
    {
        id: 2, 
        rating: 7, 
        text: "A mock API server is useful during development and testing when live data is either unavailable or unreliable. Our mock API sever guide for testing covers how you can use a mock API server so the absence of a real API doesnt hold you back."
    },
    {
        id: 3, 
        rating: 9, 
        text: "Often with internal projects, the API consumer (such as a front end developer through REST APIs) moves allows developers to consume a working API with the same interface as the eventual production API."
    }
];

export default FeedbackData;